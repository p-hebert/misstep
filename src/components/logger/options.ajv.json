{
  "required": ["logger"],
  "properties": {
    "logger": {
      "required": ["error", "warn", "info", "verbose", "debug", "silly"],
      "properties": {
        "error": { "instanceof": "Function" },
        "warn": { "instanceof": "Function" },
        "info": { "instanceof": "Function" },
        "verbose": { "instanceof": "Function" },
        "debug": { "instanceof": "Function" },
        "silly": { "instanceof": "Function" }
      }
    }
  }
}
